import React, { useCallback, useEffect, useState } from 'react'; import axios from 'axios'; import { Bot, FileCheck2 } from 'lucide-react'; import { toast } from 'sonner';
import { useAuth } from '../../context/AuthContext'; import { DoorEmpty, DoorError, DoorLoading, DoorShell } from '../../components/doors/DoorShell'; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../../components/ui/select';

export default function BoardMinutesPage() { const { API, getAuthHeaders } = useAuth(); const [items, setItems] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const load = useCallback(async () => { try { setItems((await axios.get(`${API}/api/board/minutes`, getAuthHeaders())).data.items || []); } catch (e) { setError(e?.response?.data?.detail || 'No se pudo cargar el libro.'); } finally { setLoading(false); } }, [API, getAuthHeaders]); useEffect(() => { load(); }, [load]); const update = async (item, status) => { try { await axios.put(`${API}/api/board/minutes/${item.minute_id}/status`, { status }, getAuthHeaders()); toast.success(status === 'official' ? 'Minuta marcada oficial por revisión humana' : 'Estado actualizado'); await load(); } catch (e) { toast.error(e?.response?.data?.detail || 'No se pudo revisar'); } }; if (loading) return <DoorShell eyebrow="Junta Directiva" title="Libro de minutas" description="Cargando versiones..." guideKey="board_meeting"><DoorLoading /></DoorShell>; return <DoorShell eyebrow="Junta Directiva" title="Libro de minutas" description="Versiones humanas e IA; ninguna minuta IA se vuelve oficial sin revisión." guideKey="board_meeting">{error ? <DoorError message={error} /> : <section className="space-y-3">{items.length ? items.map((item) => <article key={item.minute_id} className="border bg-white p-5" data-testid={`minutes-book-${item.minute_id}`}><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="flex items-center gap-2 font-semibold">{item.minute_type === 'ai_draft' ? <Bot className="h-4 w-4 text-[#9E8232]" /> : <FileCheck2 className="h-4 w-4" />}{item.meeting?.title || 'Reunión'}</p><p className="text-xs text-slate-500">{item.meeting?.scheduled_at ? new Date(item.meeting.scheduled_at).toLocaleString('es') : ''} · versión {item.version} · {item.minute_type}</p><div className="mt-3 max-h-48 overflow-y-auto whitespace-pre-wrap border-l-2 border-slate-200 pl-3 text-sm text-slate-600">{typeof item.content === 'string' ? item.content : JSON.stringify(item.content, null, 2)}</div></div><Select value={item.status} onValueChange={(value) => update(item, value)}><SelectTrigger className="w-full sm:w-40" data-testid={`minute-status-${item.minute_id}`}><SelectValue /></SelectTrigger><SelectContent className="bg-white"><SelectItem value="draft">Borrador</SelectItem><SelectItem value="review">En revisión</SelectItem><SelectItem value="official">Oficial</SelectItem><SelectItem value="rejected">Rechazada</SelectItem><SelectItem value="ai_draft">Borrador IA</SelectItem></SelectContent></Select></div></article>) : <DoorEmpty testId="minutes-book-empty">No hay minutas todavía.</DoorEmpty>}</section>}</DoorShell>; }