import React, { useCallback, useEffect, useState } from 'react';
import axios from 'axios';
import { Activity, AlertTriangle, CalendarCheck2, GitFork, RadioTower, UserCheck, UserPlus, UsersRound } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useAuth } from '../../context/AuthContext';
import { CellularError, CellularLoading, CellularShell, EmptyState } from '../../components/cellular/CellularShell';

const metrics = [['active_cells', 'Células activas', RadioTower], ['attendance_total', 'Asistencias', CalendarCheck2], ['new_visitors', 'Visitantes nuevos', UserPlus], ['conversions', 'Conversiones', UserCheck], ['average_per_cell', 'Promedio/célula', UsersRound], ['ready_for_cellular', 'Listos para célula', UsersRound], ['open_needs', 'Necesidades abiertas', AlertTriangle], ['multiplication_candidates', 'Candidatas', GitFork]];
export default function CellularDashboardPage() { const { API, getAuthHeaders } = useAuth(); const [data, setData] = useState(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const load = useCallback(async () => { try { setError(''); setData((await axios.get(`${API}/api/cellular/dashboard`, getAuthHeaders())).data); } catch (e) { setError(e?.response?.data?.detail || 'No se pudo cargar el dashboard celular.'); } finally { setLoading(false); } }, [API, getAuthHeaders]); useEffect(() => { load(); }, [load]); if (loading) return <CellularShell title="Dashboard celular" description="Cargando gobierno celular..." guideKey="cellular_dashboard"><CellularLoading /></CellularShell>; return <CellularShell title="Dashboard celular" description="CÉLULAS DETECTAN → PUERTAS RESPONDEN → LIDERAZGO SUPERVISA." guideKey="cellular_dashboard">{error ? <CellularError message={error} /> : <><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4" data-testid="cellular-dashboard-metrics">{metrics.map(([key, label, Icon]) => <div key={key} className="border border-[#E2D9CC] bg-white p-4 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase text-[#718096]">{label}</p><p className="mt-2 text-3xl font-semibold text-[#0F1A33]" data-testid={`cellular-metric-${key}`}>{data?.metrics?.[key] ?? 0}</p></div><Icon className="h-5 w-5 text-[#9E8232]" /></div></div>)}</section><section className="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><div><h2 className="text-lg font-semibold text-[#0F1A33]">Células en alcance</h2><div className="mt-3 grid gap-3 sm:grid-cols-2">{data?.cells?.length ? data.cells.map((cell) => <Link key={cell.cell_id} to={`/celulas/${cell.cell_id}`} className="border border-[#E2D9CC] bg-white p-4 transition-transform hover:-translate-y-0.5" data-testid={`dashboard-cell-${cell.cell_id}`}><div className="flex justify-between gap-3"><div><p className="font-semibold text-[#0F1A33]">{cell.name}</p><p className="font-mono text-xs text-[#9E8232]">{cell.code} · {cell.network_name}</p></div><Activity className="h-4 w-4 text-green-700" /></div><div className="mt-4 grid grid-cols-3 gap-2 text-center text-xs"><span><b className="block text-base">{cell.metrics.active_members}</b>Miembros</span><span><b className="block text-base">{cell.metrics.average_attendance}</b>Promedio</span><span><b className="block text-base">{cell.metrics.open_needs}</b>Necesidades</span></div></Link>) : <EmptyState testId="dashboard-cells-empty">No hay células en su alcance.</EmptyState>}</div></div><div><h2 className="text-lg font-semibold text-[#0F1A33]">Atención prioritaria</h2><div className="mt-3 space-y-3">{data?.needs?.length ? data.needs.slice(0, 6).map((need) => <Link key={need.need_id} to="/celulas/necesidades" className="block border-l-4 border-amber-500 bg-white p-3" data-testid={`dashboard-need-${need.need_id}`}><p className="text-sm font-semibold text-[#0F1A33]">{need.need_type.replaceAll('_', ' ')}</p><p className="line-clamp-2 text-xs text-[#4A5568]">{need.description}</p></Link>) : <EmptyState testId="dashboard-needs-empty">Sin necesidades abiertas en alcance.</EmptyState>}</div></div></section></>}</CellularShell>; }