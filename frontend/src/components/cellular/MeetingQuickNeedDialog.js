import React, { useState } from 'react'; import axios from 'axios'; import { AlertTriangle } from 'lucide-react'; import { toast } from 'sonner';
import { useAuth } from '../../context/AuthContext'; import { PersonSelect } from './CellularFields'; import { Button } from '../ui/button'; import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from '../ui/dialog'; import { Label } from '../ui/label'; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../ui/select'; import { Textarea } from '../ui/textarea';

const types = [['illness', 'Enfermedad'], ['urgent_prayer', 'Petición urgente'], ['crisis', 'Crisis'], ['new_believer', 'Nuevo creyente'], ['first_visit', 'Primera visita'], ['family_need', 'Necesidad familiar'], ['other', 'Otra']];
export const MeetingQuickNeedDialog = ({ cellId, meetingId, people }) => { const { API, getAuthHeaders } = useAuth(); const [open, setOpen] = useState(false); const [form, setForm] = useState({ person_id: '', need_type: 'urgent_prayer', priority: 'medium', description: '' }); const submit = async (e) => { e.preventDefault(); try { await axios.post(`${API}/api/cellular/needs`, { ...form, person_id: form.person_id || null, cell_id: cellId, meeting_id: meetingId }, getAuthHeaders()); toast.success('Necesidad enviada al flujo de respuesta'); setOpen(false); setForm({ person_id: '', need_type: 'urgent_prayer', priority: 'medium', description: '' }); } catch (error) { toast.error(error?.response?.data?.detail || 'No se pudo registrar'); } }; return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button variant="outline" className="h-12 w-full border-amber-300 bg-amber-50 text-amber-900" data-testid="meeting-open-need-button"><AlertTriangle className="mr-2 h-4 w-4" />Registrar necesidad</Button></DialogTrigger><DialogContent className="bg-white"><DialogHeader><DialogTitle>Necesidad detectada en reunión</DialogTitle><DialogDescription>Quedará vinculada a esta célula y reunión, con Puerta sugerida editable.</DialogDescription></DialogHeader><form onSubmit={submit} className="space-y-4"><PersonSelect id="meeting-need-person-select" label="Persona (opcional)" items={people || []} value={form.person_id} onChange={(value) => setForm((old) => ({ ...old, person_id: value }))} /><div className="grid grid-cols-2 gap-3"><div><Label>Tipo</Label><Select value={form.need_type} onValueChange={(value) => setForm((old) => ({ ...old, need_type: value }))}><SelectTrigger data-testid="meeting-need-type-select"><SelectValue /></SelectTrigger><SelectContent className="bg-white">{types.map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div><div><Label>Prioridad</Label><Select value={form.priority} onValueChange={(value) => setForm((old) => ({ ...old, priority: value }))}><SelectTrigger data-testid="meeting-need-priority-select"><SelectValue /></SelectTrigger><SelectContent className="bg-white"><SelectItem value="low">Baja</SelectItem><SelectItem value="medium">Media</SelectItem><SelectItem value="high">Alta</SelectItem><SelectItem value="urgent">Urgente</SelectItem></SelectContent></Select></div></div><div><Label htmlFor="meeting-need-description">Descripción</Label><Textarea id="meeting-need-description" value={form.description} onChange={(e) => setForm((old) => ({ ...old, description: e.target.value }))} required data-testid="meeting-need-description-input" /></div><Button type="submit" className="w-full bg-[#0F1A33]" data-testid="meeting-need-submit-button">Enviar a respuesta</Button></form></DialogContent></Dialog>; };