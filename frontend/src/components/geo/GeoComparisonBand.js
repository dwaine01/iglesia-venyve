import React from 'react';
import { ArrowDownRight, ArrowUpRight, Minus } from 'lucide-react';

const labels = { north: 'Norte', east: 'Este', south: 'Sur', west: 'Oeste' };
export const GeoComparisonBand = ({ comparison, compact = false }) => {
  if (!comparison) return null;
  return <section className={`border bg-slate-950 text-white shadow-2xl ${compact ? 'max-w-xl p-3' : 'p-4'}`} data-testid="geo-comparison-band"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase text-amber-300">Comparación histórica</p><h2 className={`font-['Spectral'] ${compact ? 'text-base' : 'text-xl'}`}>Últimos {comparison.days} días vs. período anterior</h2></div><p className="text-xs">Total: <b>{comparison.current_total}</b> · Anterior: {comparison.previous_total}</p></div><div className={`${compact ? 'mt-2' : 'mt-4'} grid grid-cols-4 gap-1.5`}>{Object.entries(comparison.zones || {}).map(([zone, data]) => { const Icon = data.change > 0 ? ArrowUpRight : data.change < 0 ? ArrowDownRight : Minus; return <article key={zone} className={`border border-white/15 ${compact ? 'p-2' : 'p-3'}`} data-testid={`geo-comparison-${zone}`}><span className="text-[10px] text-white/60">{labels[zone]}</span><div className="mt-1 flex items-center justify-between gap-1"><b className={compact ? 'text-base' : 'text-xl'}>{data.current}</b><span className={`text-xs ${data.change > 0 ? 'text-emerald-300' : data.change < 0 ? 'text-red-300' : 'text-white/60'}`}><Icon className="inline h-3.5 w-3.5" />{data.change > 0 ? '+' : ''}{data.change}</span></div></article>; })}</div></section>;
};