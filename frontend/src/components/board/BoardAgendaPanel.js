import React, { useState } from 'react'; import axios from 'axios'; import { CheckCircle2, Play, Plus } from 'lucide-react'; import { toast } from 'sonner';
import { useAuth } from '../../context/AuthContext'; import { Button } from '../ui/button'; import { Input } from '../ui/input'; import { Label } from '../ui/label'; import { Textarea } from '../ui/textarea';

export const BoardAgendaPanel = ({ meeting, onReload }) => { const { API, getAuthHeaders } = useAuth(); const [form, setForm] = useState({ title: '', description: '', estimated_minutes: 10 }); const update = async (item, payload) => { try { await axios.put(`${API}/api/board/agenda/${item.agenda_item_id}`, payload, getAuthHeaders()); await onReload(); } catch (e) { toast.error(e?.response?.data?.detail || 'No se pudo actualizar'); } }; const add = async (e) => { e.preventDefault(); try { await axios.post(`${API}/api/board/meetings/${meeting.meeting_id}/agenda`, { ...form, estimated_minutes: Number(form.estimated_minutes), voting_enabled: false }, getAuthHeaders()); setForm({ title: '', description: '', estimated_minutes: 10 }); await onReload(); } catch (error) { toast.error(error?.response?.data?.detail || 'No se pudo añadir'); } }; return <section className="space-y-3" data-testid="board-agenda-panel">{meeting.agenda.map((item) => <article key={item.agenda_item_id} className="border bg-white p-4" data-testid={`board-agenda-${item.agenda_item_id}`}><div className="flex items-start justify-between gap-3"><div><p className="font-mono text-xs text-[#9E8232]">{String(item.order).padStart(2, '0')} · {item.estimated_minutes} min</p><h3 className="font-semibold">{item.title}</h3><p className="text-xs text-slate-500">{item.description}</p></div><span className="text-xs uppercase">{item.status}</span></div><div className="mt-3 flex gap-2">{item.status === 'pending' && <Button size="sm" onClick={() => update(item, { status: 'in_progress', started_at: new Date().toISOString() })} data-testid={`start-agenda-${item.agenda_item_id}`}><Play className="mr-1 h-3 w-3" />Iniciar</Button>}{item.status === 'in_progress' && <Button size="sm" onClick={() => update(item, { status: 'completed', ended_at: new Date().toISOString() })} data-testid={`complete-agenda-${item.agenda_item_id}`}><CheckCircle2 className="mr-1 h-3 w-3" />Completar</Button>}</div></article>)}<form onSubmit={add} className="space-y-3 border border-dashed bg-white p-4"><h3 className="font-semibold">Añadir punto</h3><div><Label htmlFor="new-agenda-title">Título</Label><Input id="new-agenda-title" value={form.title} onChange={(e) => setForm((old) => ({ ...old, title: e.target.value }))} required data-testid="new-agenda-title-input" /></div><div><Label htmlFor="new-agenda-description">Descripción</Label><Textarea id="new-agenda-description" value={form.description} onChange={(e) => setForm((old) => ({ ...old, description: e.target.value }))} data-testid="new-agenda-description-input" /></div><Button type="submit" variant="outline" data-testid="add-agenda-submit-button"><Plus className="mr-2 h-4 w-4" />Añadir a agenda</Button></form></section>; };