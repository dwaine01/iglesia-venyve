import React, { useState } from 'react'; import axios from 'axios'; import { UserPlus } from 'lucide-react'; import { toast } from 'sonner';
import { useAuth } from '../../context/AuthContext'; import { PersonSelect } from '../cellular/CellularFields'; import { Button } from '../ui/button'; import { Checkbox } from '../ui/checkbox'; import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from '../ui/dialog'; import { Label } from '../ui/label'; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../ui/select';

const permissionOptions = [['board.read', 'Consultar Junta'], ['board.meetings.write', 'Administrar reuniones'], ['board.notes.write', 'Notas y minuta manual'], ['board.vote', 'Crear y votar propuestas'], ['board.actions.write', 'Acuerdos y tareas'], ['board.audio.manage', 'Grabar y corregir transcript'], ['board.minutes.review', 'Revisar minuta IA'], ['board.audit.read', 'Consultar auditoría'], ['doors.assignments.manage', 'Asignar equipos de Puerta']];
export const BoardMemberDialog = ({ people, positions, doors, onSaved }) => { const { API, getAuthHeaders } = useAuth(); const [open, setOpen] = useState(false); const [form, setForm] = useState({ person_id: '', position_key: '', voting_rights: true, permissions: ['board.read', 'board.vote'], supervised_door_keys: [], ministry_ids: [] }); const toggle = (field, value) => setForm((old) => ({ ...old, [field]: old[field].includes(value) ? old[field].filter((item) => item !== value) : [...old[field], value] })); const submit = async (e) => { e.preventDefault(); try { await axios.post(`${API}/api/board/members`, form, getAuthHeaders()); toast.success('Miembro incorporado a la Junta'); setOpen(false); await onSaved(); } catch (error) { toast.error(error?.response?.data?.detail || 'No se pudo incorporar'); } }; return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button className="bg-[#C8A951] text-[#14213D]" data-testid="open-add-board-member-button"><UserPlus className="mr-2 h-4 w-4" />Añadir miembro</Button></DialogTrigger><DialogContent className="max-h-[90vh] overflow-y-auto bg-white"><DialogHeader><DialogTitle>Membresía formal de Junta</DialogTitle><DialogDescription>Cargo, voto, permisos y Puertas quedan versionados por Persona.</DialogDescription></DialogHeader><form onSubmit={submit} className="space-y-5"><PersonSelect id="board-member-person-select" label="Persona" items={people} value={form.person_id} onChange={(value) => setForm((old) => ({ ...old, person_id: value }))} /><div><Label>Cargo configurable</Label><Select value={form.position_key} onValueChange={(value) => setForm((old) => ({ ...old, position_key: value }))}><SelectTrigger data-testid="board-member-position-select"><SelectValue placeholder="Seleccionar cargo" /></SelectTrigger><SelectContent className="bg-white">{positions.map((item) => <SelectItem key={item.position_key} value={item.position_key}>{item.name}</SelectItem>)}</SelectContent></Select></div><label className="flex min-h-12 items-center gap-3 border p-3 text-sm"><Checkbox checked={form.voting_rights} onCheckedChange={(value) => setForm((old) => ({ ...old, voting_rights: value }))} data-testid="board-member-voting-checkbox" />Tiene derecho a voto</label><fieldset><legend className="text-sm font-medium">Permisos</legend><div className="mt-2 grid gap-2 sm:grid-cols-2">{permissionOptions.map(([value, label]) => <label key={value} className="flex min-h-11 items-center gap-2 border p-2 text-xs"><Checkbox checked={form.permissions.includes(value)} onCheckedChange={() => toggle('permissions', value)} data-testid={`board-permission-${value.replaceAll('.', '-')}`} />{label}</label>)}</div></fieldset><fieldset><legend className="text-sm font-medium">Puertas supervisadas</legend><div className="mt-2 grid gap-2 sm:grid-cols-3">{doors.map((door) => <label key={door.door_key} className="flex min-h-11 items-center gap-2 border p-2 text-xs"><Checkbox checked={form.supervised_door_keys.includes(door.door_key)} onCheckedChange={() => toggle('supervised_door_keys', door.door_key)} data-testid={`board-door-${door.door_key}`} />{door.number}. {door.name}</label>)}</div></fieldset><Button type="submit" disabled={!form.person_id || !form.position_key} className="w-full bg-[#14213D]" data-testid="add-board-member-submit-button">Guardar membresía</Button></form></DialogContent></Dialog>; };